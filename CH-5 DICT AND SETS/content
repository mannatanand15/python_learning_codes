DICTIONARY:-
dictionary is stored as key:value
harry acts as key and 100 acts as value
mutable,connot contain duplicate keys,onordered,indexed.
  d={}  #empty dictionary
  
marks={
    "harry":100,
    "shubham":50,
    "rohan":76
}
print(marks)
print(type(marks))
print(marks.keys())
print(marks.values())

  SETS:
empty set is not created by set={} as this creates an empty dictionay
to create empty set
